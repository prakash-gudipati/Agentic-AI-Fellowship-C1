# Session 23 — Retrieval Techniques II
# Copy this file to `.env` and paste your real keys (never commit `.env`).

# OpenAI key — used by embeddings.py for both ingest and query embeddings.
OPENAI_API_KEY=sk-...

# Anthropic key — used by retrievers/rerank.py for the LLM-as-judge reranker.
ANTHROPIC_API_KEY=sk-ant-...
