# Production dependencies for the S16 wrapper extension.
# Adds pydantic on top of the S15 set.

openai>=1.50.0
anthropic>=0.40.0
tenacity>=9.0.0
python-dotenv>=1.0.0
tiktoken>=0.7.0
pydantic>=2.0.0

# Test dependencies
pytest>=8.0.0
