# Session 30 — both keys are needed. The whole point of the session is that
# the same agent runs on either provider once tools are described with a
# proper JSON schema. The demo runs that contrast on screen.
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
